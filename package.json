{
  "name": "junre",
  "version": "0.1.0",
  "homepage": "https://zapicojunredexter.github.io/portfolio",
  "private": true,
  "dependencies": {
    "@testing-library/dom": "^10.4.0",
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.3.0",
    "@testing-library/user-event": "^13.5.0",
    "@types/jest": "^27.5.2",
    "@types/node": "^16.18.126",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "pixelarticons": "^2.4.1",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-scripts": "5.0.1",
    "typescript": "^4.9.5",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy-manual": "echo '⚠️ WARNING: GitHub Actions auto-deploy is active. Manual deploy is backup only.' && npm run build && cp -r build/* . && git add . && (git commit -m 'Manual deploy to GitHub Pages (backup)' || echo 'No changes to commit') && git push --set-upstream origin main",
    "deploy": "echo '✅ Using GitHub Actions for deployment. Push your changes to trigger auto-deploy!' && git status"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
